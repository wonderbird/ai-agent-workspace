---
description: Use when evaluating mutation test scores
alwaysApply: false
---
# Stryker Mutator Usage

Whenever you want to analyze mutations, you must instruct stryker to output a JSON formatted report:

| Programming Language | Command to run Stryker Mutator   | JSON report location                                                                                                                                   |
| -------------------- | -------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------ |
| C#                   | `dotnet stryker --reporter json` | `StrykerOutput/YYYY-MM-DD.hh-mm-ss/reports/mutation-report.json` where `YYYY-MM-DD.hh-mm-ss` represents the system time when the report was generated. |
| JavaScript           | `npm run test:mutate`            | `reports/mutation/mutation.json`                                                                                                                       |

The primary tool to analyse the JSON report is `jq`.

Fall back to standard text processing tools if `jq` does not return the expected output.

## Happiness

🧟
